int main() {
    int a = 10;

    // Змінна 'a' має тип 'int', а літерал '20' - теж 'int'.
    // Компілятор повинен обчислити це як 'true' (1).
    int result = a === 20;
    print(result); // Повинно вивести 1

    return 0;
}