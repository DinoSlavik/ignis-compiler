int main() {
    int y = {
        mut int z = 10 + 5; // z = 15
        z = z + 20;         // z = 35
        z                   // Немає ';', це значення повертається з блоку
    };
    print(y); // Повинно вивести 35

    // Неявний return з main
    0
}