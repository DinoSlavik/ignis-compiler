int main() {
    int a = 10;
    int b = 20;

    if (a > 5 and b > 15) {
        print(1); // Цей блок має виконатись
    } else {
        print(0);
    };

    if (a > 15 or b > 25) {
        print(0);
    } else {
        print(1); // Цей блок має виконатись
    };

    print(1 xor 1);

    return 0;
}