int main() {
    mut int i = 0;

    while (i < 11) {
        int a = {
            if (i > 0) {
                mut int j = 0;
                mut int r = i;
                while (j < i) {
                    r = r * i;
                    j = j + 1;
                    //print(j);
                }
                r
            }
            else {
                // Was just `1` instead of what there is now
                // TODO: fix this, please, it is stupid.
                int ret1 = 1;
                ret1
            };
        };
        print(a); putchar('\n');
        i = i + 1;
    }

    return 0;
}